pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // For dev.jeziellago:compose-markdown - published on JitPack only.
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Stat Up"
include ':app'
